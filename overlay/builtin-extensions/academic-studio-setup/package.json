{
  "name": "academic-studio-setup",
  "displayName": "Academic Studio Setup",
  "description": "First-run setup for Academic Studio: pick your audience and choose which bundled extensions are active.",
  "version": "1.0.0",
  "publisher": "academic-studio",
  "license": "MIT",
  "engines": { "vscode": "^1.121.0" },
  "categories": ["Other"],
  "main": "./extension.js",
  "activationEvents": ["onStartupFinished"],
  "_extensionKindNote": "See the same note in academic-studio-defaults. Setup manages THIS machine's app — bundled extensions, installed programs — so the local extension host is the right place for it in a remote window, and without 'ui' it would not run there at all.",
  "extensionKind": ["workspace", "ui"],
  "contributes": {
    "commands": [
      {
        "command": "academicStudio.openSetup",
        "title": "Academic Studio Setup…",
        "category": "Academic Studio"
      }
    ]
  }
}
